# prefix.py.example -  请复制此文件并重命名为 prefix.py，然后根据你的需要修改以下内容

def get_original_prefix():
    """
    返回用于生成 AI 回答的初始提示前缀。

    请根据你的实际情况修改此函数的return值，以提供更准确和个性化的上下文信息。
    """
    return """以下是转录文字（可能因识别而存在部分错误），请你结合上下文，总结最新情况。

    """